# 管理员端用户管理模块功能说明文档

## 一、功能说明

管理员端用户管理模块是系统的核心管理功能之一，主要用于管理员对平台所有用户进行集中管理和监控。该模块提供了用户列表查询、搜索、分页和删除等功能，帮助管理员高效管理平台用户，维护平台秩序和安全。

### 核心功能
1. **用户列表查询**：分页展示平台所有用户信息，包括用户基本信息、角色、创建时间等
2. **用户搜索**：支持根据用户账号和用户名进行模糊搜索
3. **用户删除**：允许管理员删除违规或无效用户账号
4. **用户数据统计**：在数据分析大屏中展示用户增长趋势和总用户量

## 二、用例说明

### 1. 用户列表查询用例
- **场景**：管理员需要查看平台所有用户的基本信息
- **执行者**：管理员
- **前置条件**：管理员已登录系统，拥有管理员权限
- **流程**：管理员进入用户管理页面，系统自动加载并展示用户列表
- **结果**：管理员可以查看所有用户的详细信息，包括账号、用户名、头像、简介、角色和创建时间

### 2. 用户搜索用例
- **场景**：管理员需要查找特定用户
- **执行者**：管理员
- **前置条件**：管理员已登录系统，拥有管理员权限
- **流程**：管理员在搜索框中输入用户账号或用户名，点击搜索按钮
- **结果**：系统根据搜索条件过滤并展示匹配的用户列表

### 3. 用户删除用例
- **场景**：管理员需要删除违规或无效用户账号
- **执行者**：管理员
- **前置条件**：管理员已登录系统，拥有管理员权限
- **流程**：管理员在用户列表中找到目标用户，点击删除按钮，系统执行删除操作
- **结果**：用户账号被删除，系统刷新用户列表，显示删除成功提示

### 4. 用户数据统计用例
- **场景**：管理员需要查看平台用户增长情况和总用户量
- **执行者**：管理员
- **前置条件**：管理员已登录系统，拥有管理员权限
- **流程**：管理员进入数据分析大屏，系统自动加载并展示用户相关统计数据
- **结果**：管理员可以查看当前总用户量和近15日用户增长趋势

## 三、操作流程

### 1. 用户列表查询流程
1. 管理员登录系统
2. 进入后台管理页面
3. 点击"用户管理"菜单
4. 系统调用`listUserVoByPage`接口获取用户列表数据
5. 系统在表格中展示用户列表，包括id、账号、用户名、头像、简介、角色和创建时间
6. 管理员可以通过分页控件切换页码或调整每页显示条数

### 2. 用户搜索流程
1. 管理员在用户管理页面的搜索框中输入搜索关键词（用户账号或用户名）
2. 点击"搜索"按钮
3. 系统重置页码为1
4. 调用`listUserVoByPage`接口，传入搜索条件
5. 系统在表格中展示搜索结果

### 3. 用户删除流程
1. 管理员在用户列表中找到需要删除的用户
2. 点击该用户对应的"删除"按钮
3. 系统调用`deleteUser`接口，传入用户id
4. 接口验证管理员权限
5. 系统从数据库中删除该用户记录
6. 系统返回删除成功响应
7. 前端显示删除成功提示
8. 系统自动刷新用户列表

### 4. 用户数据统计流程
1. 管理员登录系统
2. 进入数据分析大屏页面
3. 系统调用相关接口获取用户统计数据
4. 系统在大屏上展示当前总用户量和近15日用户增长趋势
5. 系统实时更新当前时间

## 四、数据表字段说明

### 用户表（user）

| 字段名 | 类型 | 描述 | 约束 |
|--------|------|------|------|
| id | Long | 用户唯一标识 | 主键，自增 |
| userAccount | String | 用户登录账号 | 唯一，非空 |
| userPassword | String | 用户登录密码 | 非空，MD5加盐加密存储 |
| userName | String | 用户显示名称 | 默认为"默认名称" |
| userAvatar | String | 用户头像URL | 可为空 |
| userProfile | String | 用户个人简介 | 可为空 |
| userRole | String | 用户角色 | 非空，默认值为"user"，可选值："user"（普通用户）、"admin"（管理员） |
| editTime | Date | 编辑时间 | 自动更新 |
| createTime | Date | 创建时间 | 自动生成 |
| updateTime | Date | 更新时间 | 自动更新 |
| isDelete | Integer | 是否删除 | 逻辑删除标识，0：未删除，1：已删除 |

## 五、技术实现

### 前端实现
- **页面组件**：UserManagePage.vue（用户管理）、DataAnalysisPageV2.vue（数据分析）
- **技术栈**：Vue 3 + TypeScript + Ant Design Vue
- **核心API**：
  - `listUserVoByPage`：分页查询用户列表
  - `deleteUser`：删除用户

### 后端实现
- **控制器**：UserController.java
- **服务实现**：UserServiceImpl.java
- **权限控制**：所有管理员功能需要`admin`角色权限
- **数据访问**：基于MyBatis Plus的数据库操作

## 六、权限说明

| 功能 | 所需角色 | 权限说明 |
|------|----------|----------|
| 用户列表查询 | admin | 仅管理员可查看所有用户信息 |
| 用户搜索 | admin | 仅管理员可搜索用户 |
| 用户删除 | admin | 仅管理员可删除用户 |
| 用户数据统计 | admin | 仅管理员可查看用户统计数据 |

## 七、安全考虑

1. **密码安全**：用户密码采用MD5加盐加密存储，确保密码安全
2. **权限控制**：所有管理员功能均需要验证管理员权限，防止越权操作
3. **数据脱敏**：返回给前端的用户信息已进行脱敏处理，保护用户隐私
4. **操作日志**：系统记录管理员的重要操作，便于审计和追溯

## 八、总结

管理员端用户管理模块是平台运营和管理的重要工具，通过该模块管理员可以高效管理平台用户，维护平台秩序和安全。该模块功能简洁明了，操作流程清晰，能够满足平台日常运营的基本需求。未来可以考虑扩展更多功能，如用户编辑、角色管理、批量操作等，进一步提升管理员的工作效率和管理能力。