# 管理员端评论管理模块功能描述

## 一、模块概述

管理员端评论管理模块是系统的重要管理功能之一，主要用于管理员对平台所有用户评论进行集中管理和监控。该模块提供了评论列表查询、评论详情查看、评论审核、回复、删除、状态管理等功能，是平台内容质量控制和用户互动管理的重要组成部分，帮助管理员高效管理平台评论体系，维护良好的用户交流环境。

## 二、核心功能

### 1. 评论列表查询
- **功能描述**：分页展示平台所有评论信息，支持按状态、时间、内容、用户等条件筛选和搜索
- **实现方式**：通过`getAllComments`接口获取所有评论数据，支持分页、排序和多条件筛选
- **展示内容**：评论ID、内容、用户信息、关联内容、状态、创建时间、回复情况、点赞数
- **筛选条件**：评论状态（全部/待审核/已通过/已驳回）、时间范围、关键词搜索、用户ID、关联内容类型
- **权限控制**：仅管理员可访问所有评论

### 2. 评论详情查看
- **功能描述**：允许管理员查看单条评论的详细信息，包括评论上下文和相关数据
- **实现方式**：通过`getCommentDetail`接口获取评论详细数据
- **展示内容**：评论完整内容、用户信息、关联内容详情、创建时间、IP地址、设备信息、回复记录
- **权限控制**：仅管理员可使用

### 3. 评论审核
- **功能描述**：对用户提交的评论进行审核，决定是否允许在平台展示
- **实现方式**：通过`auditComment`接口更新评论状态，支持通过和驳回两种操作
- **审核选项**：
  - 通过：将评论状态设置为已通过，允许在前端展示
  - 驳回：将评论状态设置为已驳回，不在前端展示
- **审核结果通知**：可选择是否通过系统消息通知用户审核结果
- **权限控制**：仅管理员可使用

### 4. 评论回复
- **功能描述**：允许管理员对用户评论进行官方回复，增强平台与用户的互动
- **实现方式**：通过`replyComment`接口添加评论回复
- **回复内容**：支持富文本编辑，可插入图片和链接
- **回复显示**：管理员回复会显示官方标识，优先展示
- **权限控制**：仅管理员可使用

### 5. 评论删除
- **功能描述**：允许管理员删除违规或不合适的评论
- **实现方式**：通过`deleteComment`接口删除评论，支持单条删除和批量删除
- **删除类型**：
  - 软删除：标记评论为已删除，保留记录便于审计
  - 硬删除：永久删除评论数据（谨慎使用）
- **操作流程**：选择评论 → 点击删除按钮 → 二次确认 → 执行删除操作 → 返回删除结果
- **权限控制**：仅管理员可使用

### 6. 评论状态管理
- **功能描述**：允许管理员修改评论的状态，控制评论是否在前端展示
- **实现方式**：通过`updateCommentStatus`接口更新评论状态
- **状态类型**：
  - 待审核（0）：新提交的评论，等待管理员审核
  - 已通过（1）：审核通过的评论，可在前端正常展示
  - 已驳回（2）：审核不通过的评论，不在前端展示
  - 已删除（3）：被删除的评论，不在前端展示
- **状态流转**：待审核 ↔ 已通过 ↔ 已驳回 ↔ 已删除
- **权限控制**：仅管理员可使用

### 7. 评论统计
- **功能描述**：提供评论数据统计分析，帮助管理员了解平台评论情况
- **实现方式**：通过`getCommentStatistics`接口获取评论统计数据
- **统计维度**：
  - 按时间：今日/昨日/本周/本月评论数量
  - 按状态：各状态评论数量分布
  - 按内容类型：不同内容类型的评论数量
  - 热门评论：点赞数最高的评论排行
- **展示方式**：图表展示（柱状图、饼图）+ 数据表格
- **权限控制**：仅管理员可使用

## 三、技术实现

### 前端实现
- **页面组件**：评论管理相关页面（待实现）
- **技术栈**：Vue 3 + TypeScript + Ant Design Vue
- **核心API**：
  - `getAllComments`：获取评论列表，支持分页和筛选
  - `getCommentDetail`：获取评论详情
  - `auditComment`：审核评论
  - `replyComment`：回复评论
  - `deleteComment`：删除评论
  - `updateCommentStatus`：更新评论状态
  - `getCommentStatistics`：获取评论统计数据

### 后端实现
- **控制器**：CommentController.java
- **服务实现**：CommentServiceImpl.java
- **权限控制**：基于角色的访问控制（RBAC）
- **数据访问**：基于MyBatis Plus的数据库操作
- **API列表**：
  | API路径 | 方法 | 功能描述 | 权限要求 |
  |---------|------|----------|----------|
  | /comment/list | GET | 获取评论列表，支持分页和筛选 | 管理员 |
  | /comment/detail/{id} | GET | 获取评论详情 | 管理员 |
  | /comment/audit | POST | 审核评论 | 管理员 |
  | /comment/reply | POST | 回复评论 | 管理员 |
  | /comment/delete | POST | 删除评论 | 管理员 |
  | /comment/update-status | POST | 更新评论状态 | 管理员 |
  | /comment/statistics | GET | 获取评论统计数据 | 管理员 |

## 四、权限说明

| 功能 | 所需角色 | 权限说明 |
|------|----------|----------|
| 评论列表查询 | admin | 仅管理员可查看所有评论 |
| 评论详情查看 | admin | 仅管理员可查看评论详情 |
| 评论审核 | admin | 仅管理员可审核评论 |
| 评论回复 | admin | 仅管理员可回复评论 |
| 评论删除 | admin | 仅管理员可删除评论 |
| 评论状态管理 | admin | 仅管理员可修改评论状态 |
| 评论统计 | admin | 仅管理员可查看评论统计数据 |

## 五、安全考虑

1. **权限控制**：基于角色的访问控制，确保只有授权用户才能执行敏感操作
2. **数据验证**：所有API接口均有参数校验，防止恶意请求
3. **内容安全**：评论内容需经过安全过滤，防止XSS攻击和敏感信息
4. **操作日志**：系统记录管理员的重要操作，便于审计和追溯
5. **删除机制**：支持软删除和硬删除两种方式，便于数据恢复和审计
6. **IP追踪**：记录评论发布者的IP地址和设备信息，便于违规追踪

## 六、使用流程

### 1. 评论列表查询流程
1. 管理员登录系统
2. 进入后台管理页面
3. 点击"评论管理"菜单
4. 系统调用`getAllComments`接口获取评论数据
5. 系统在表格中展示评论列表，包括评论内容、用户、状态、创建时间等
6. 管理员可以使用筛选条件和搜索功能查找特定评论

### 2. 评论审核流程
1. 管理员在评论列表中找到待审核的评论
2. 点击"审核"按钮或进入评论详情页
3. 查看评论内容和相关信息
4. 选择审核结果（通过/驳回）
5. 点击"提交"按钮
6. 系统调用`auditComment`接口，更新评论状态
7. 系统返回审核成功响应
8. 前端显示审核成功提示
9. 系统自动刷新评论列表

### 3. 评论回复流程
1. 管理员在评论详情页点击"回复"按钮
2. 系统弹出回复编辑框
3. 管理员输入回复内容
4. 点击"发送"按钮
5. 系统调用`replyComment`接口，添加回复记录
6. 系统返回回复成功响应
7. 前端显示回复成功提示
8. 系统自动刷新评论详情

### 4. 评论删除流程
1. 管理员在评论列表中选择需要删除的评论
2. 点击"删除"按钮
3. 系统弹出二次确认对话框
4. 管理员确认删除操作
5. 系统调用`deleteComment`接口，执行删除操作
6. 系统返回删除成功响应
7. 前端显示删除成功提示
8. 系统自动刷新评论列表

## 七、业务规则

1. **评论状态规则**：
   - 新提交的评论默认状态为"待审核"
   - 只有"已通过"状态的评论才能在前端展示
   - 评论状态变更需记录操作人和操作时间

2. **审核规则**：
   - 管理员需在规定时间内完成评论审核
   - 审核驳回需填写驳回原因
   - 敏感内容自动标记为待审核

3. **删除规则**：
   - 违规评论必须删除
   - 删除操作需谨慎，建议优先使用软删除
   - 批量删除操作有数量限制

4. **回复规则**：
   - 管理员回复需保持官方、友好的语气
   - 回复内容需符合平台规范
   - 管理员回复会自动推送给评论用户

5. **数据安全性**：
   - 所有API接口均需经过权限验证
   - 敏感操作（如删除）需要二次确认
   - 操作日志记录，便于审计和追溯

## 八、总结

管理员端评论管理模块是平台内容质量控制和用户互动管理的重要工具，通过该模块管理员可以高效管理平台评论体系，维护良好的用户交流环境。该模块功能完善，操作流程清晰，能够满足平台日常运营的需求。未来可以考虑扩展更多功能，如智能审核、关键词过滤、批量操作优化、评论导出等，进一步提升管理员的工作效率和平台评论管理能力。