# 导入错误修复说明

## 🐛 错误信息

```
ArticleEditPage.vue:138 Uncaught SyntaxError: 
The requested module '/src/api/articleController.ts' does not provide an export named 'getArticleCategories'
```

## 🔍 问题原因

`getArticleCategories` 函数应该从 `articleCategoryController.ts` 导入，而不是从 `articleController.ts` 导入。

## ✅ 已修复的文件

### 1. `ArticleEditPage.vue`

**修改前：**
```typescript
import {
  getArticleDetail,
  addArticle,
  updateArticle
} from '@/api/articleController'
import { getArticleCategories } from '@/api/articleController'  // ❌ 错误
```

**修改后：**
```typescript
import {
  getArticleDetail,
  addArticle,
  updateArticle,
  publishArticle
} from '@/api/articleController'
import { getCategories } from '@/api/articleCategoryController'  // ✅ 正确
```

**函数调用修改：**
```typescript
// 修改前
const res = await getArticleCategories()  // ❌

// 修改后
const res = await getCategories()  // ✅
```

### 2. `ArticleManagePage.vue`

你已经正确修改了这个文件：
```typescript
import { getCategories } from '@/api/articleCategoryController'  // ✅ 正确
```

### 3. `articleController.ts`

重新添加了 `offlineArticle` 方法（因为你删除了它）：
```typescript
/** 下架文章 POST /article/offline/${param0} */
export async function offlineArticle(
  params: { id: number },
  options?: { [key: string]: any }
) {
  const { id: param0 } = params
  return request<API.BaseResponseBoolean>(`/article/offline/${param0}`, {
    method: 'POST',
    ...(options || {}),
  })
}
```

## 📊 API 导入规则

### 文章分类相关 → `articleCategoryController.ts`
```typescript
import { getCategories } from '@/api/articleCategoryController'
```

### 文章相关 → `articleController.ts`
```typescript
import {
  getArticleList,
  getArticleDetail,
  addArticle,
  updateArticle,
  deleteArticle,
  publishArticle,
  offlineArticle,
  likeArticle,
  collectArticle,
  cancelCollectArticle,
  getHotArticles,
  getRecommendArticles,
  searchArticles
} from '@/api/articleController'
```

## ⚠️ 关于数据库字段映射

### 问题：你的数据库字段是什么格式？

#### 情况1：驼峰式（如 `categoryId`, `isDelete`）
```yaml
# application.yml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true  # ✅ 设置为 true
```

#### 情况2：下划线式（如 `category_id`, `is_delete`）
```yaml
# application.yml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false  # ✅ 设置为 false
```

### 当前配置
你的 `application.yml` 中是 `false`，这意味着：
- 数据库字段应该是下划线式：`category_id`, `is_delete`
- 如果你的数据库字段是驼峰式，需要改为 `true`

### 如何检查？

运行以下 SQL 查看你的表结构：
```sql
SHOW CREATE TABLE article;
SHOW CREATE TABLE article_category;
```

如果看到的是：
- `category_id` → 保持 `map-underscore-to-camel-case: false`
- `categoryId` → 改为 `map-underscore-to-camel-case: true`

## ✅ 现在应该可以正常运行了

### 测试步骤

1. **刷新浏览器**（Ctrl + F5 强制刷新）

2. **访问文章管理页面**
   ```
   http://localhost:5173/admin/article/manage
   ```

3. **点击"新建文章"**
   - 应该能正常打开编辑页面
   - 分类下拉框应该有数据

4. **检查控制台**
   - 不应该有导入错误
   - API 调用应该正常

## 🔧 如果还有问题

### 问题1：分类下拉框没有数据
**检查**：
- 后端 `/article/category/list` 接口是否正常
- 数据库 `article_category` 表是否有数据
- 浏览器 Network 标签查看请求状态

### 问题2：保存文章失败
**检查**：
- 后端日志查看错误信息
- 数据库字段映射是否正确
- 必填项是否都填写了

### 问题3：页面还是报错
**解决**：
1. 清空浏览器缓存
2. 重启前端服务：`npm run dev`
3. 检查所有导入语句是否正确

## 📝 总结

### 修改的文件
- ✅ `ArticleEditPage.vue` - 修复导入语句
- ✅ `articleController.ts` - 添加 `offlineArticle` 方法

### 正确的导入方式
```typescript
// 文章分类
import { getCategories } from '@/api/articleCategoryController'

// 文章操作
import {
  getArticleDetail,
  addArticle,
  updateArticle,
  publishArticle,
  offlineArticle
} from '@/api/articleController'
```

---

**修复日期**: 2025-11-26  
**状态**: ✅ 已修复

