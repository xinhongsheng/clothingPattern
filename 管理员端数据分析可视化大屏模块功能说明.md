# 管理员端数据分析可视化大屏模块功能说明

## （1）功能说明

数据分析可视化大屏模块是服装图案智能创作平台的核心数据展示功能，为管理员和设计师提供直观的数据洞察。该模块通过多种图表形式，实时展示平台的关键运营数据，包括用户增长趋势、目标人群分布、热门风格排行等，帮助平台运营者把握市场趋势，优化产品策略。

大屏包含六个核心数据可视化组件：
- 近15日用户增长量（折线图）：展示最近15天的用户注册增长趋势
- 目标人群top5（柱状图）：统计平台图案最受欢迎的五大目标人群
- 近七天文章互动数据（组合图表）：分别展示近7天文章的点赞、浏览量、收藏量top5
- 热门风格top5（饼图/柱状图）：统计最受欢迎的五大图案风格
- 用户群体作品偏好趋势（折线图）：展示不同用户群体一周内的作品偏好变化
- 作品互动量top5（柱状图）：按互动量排序的热门作品榜单

## （2）用例说明

数据分析可视化大屏主要服务于两类用户：

**管理员视角**：
- 实时监控平台运营状况，了解用户增长趋势和活跃情况
- 分析热门风格和目标人群，优化平台内容审核和推荐策略
- 评估平台内容质量，通过作品互动量识别优质内容

**设计师视角**：
- 洞察市场流行趋势，了解当前最受欢迎的风格和主题
- 分析目标人群偏好，为创作方向提供数据支持
- 学习优质作品特征，提升自身创作水平

## （3）操作流程

### 数据加载流程
1. 管理员/设计师登录系统，进入数据分析可视化大屏页面
2. 前端自动发送请求到后端API，获取各类统计数据
3. 后端从数据库中查询原始数据，进行统计分析和数据处理
4. 后端将处理后的结构化数据返回给前端
5. 前端使用ECharts库将数据渲染为各类图表
6. 图表加载完成后，展示完整的可视化大屏

### 具体图表数据获取流程

#### 1. 近15日用户增长量
- 前端请求：`GET /home/user/growth`
- 后端处理：查询近15天每天的用户注册数量
- 数据返回：包含日期和用户数量的列表
- 前端渲染：折线图，展示每日新增用户变化趋势

#### 2. 目标人群top5
- 前端请求：`GET /home/target/topFive`
- 后端处理：统计所有图案的目标人群字段，按数量排序取前5
- 数据返回：包含目标人群名称和数量的列表
- 前端渲染：柱状图，展示排名前5的目标人群分布

#### 3. 近七天文章互动数据
- 前端请求：`GET /home/article/interaction`
- 后端处理：分别统计近7天文章的点赞、浏览量、收藏量，各取前5
- 数据返回：包含文章ID、标题、各类互动数据的列表
- 前端渲染：组合图表，分别展示三种互动数据的top5排行

#### 4. 热门风格top5
- 前端请求：`GET /home/style/hot`
- 后端处理：统计所有图案的风格字段，按数量排序取前5
- 数据返回：包含风格名称和数量的列表
- 前端渲染：饼图或柱状图，展示最受欢迎的5种风格

#### 5. 用户群体作品偏好趋势
- 前端请求：`GET /home/style/preference`
- 后端处理：按用户群体分组，统计一周内各风格的作品数量变化
- 数据返回：包含日期、用户群体、风格、作品数量的四维数据
- 前端渲染：折线图，展示不同用户群体的风格偏好变化趋势

#### 6. 作品互动量top5
- 前端请求：`GET /home/interaction`
- 后端处理：统计所有图案的互动量（点赞+评论+收藏），按总量排序取前5
- 数据返回：包含图案ID、名称、互动量的列表
- 前端渲染：柱状图，展示互动量最高的5个作品

## （4）数据库表字段

### 核心数据实体

#### 5.1.1 用户表（user）
- id：用户ID（主键）
- userAccount：账号（唯一）
- userPassword：密码（加密）
- userName：昵称
- userAvatar：头像URL
- userProfile：简介
- userRole：角色（user/admin）
- createTime、updateTime、isDelete

#### 5.1.2 图案表（pattern）
- id：图案ID（主键）
- userId：创作者ID
- patternName：图案名称
- description：图案描述
- generationType：生成类型（TEXT_GENERATED/IMAGE_REFERENCED/MJ_GENERATED）
- referenceImageUrl：参考图片URL
- patternUrl：图案URL
- thumbUrl：缩略图URL
- style：风格
- season：季节
- targetAudience：目标受众
- generationParams：生成参数（JSON）
- auditStatus：审核状态（PENDING/APPROVED/REJECTED）
- auditTime、auditorId、rejectReason
- likeCount：点赞数
- createTime、updateTime、isDelete

#### 5.1.3 文章表（article）
- id：文章ID（主键）
- title：标题
- content：内容
- coverImage：封面图片URL
- category：分类
- viewCount：浏览量
- likeCount：点赞数
- collectCount：收藏数
- createTime、updateTime、isDelete

#### 5.1.4 点赞表（user_like）
- id：点赞ID（主键）
- userId：用户ID
- patternId：图案ID（可为null，与articleId二选一）
- articleId：文章ID（可为null，与patternId二选一）
- createTime、updateTime、isDelete

#### 5.1.5 收藏表（user_collect）
- id：收藏ID（主键）
- userId：用户ID
- articleId：文章ID
- createTime、updateTime、isDelete

## （5）技术实现要点

### 前端实现
- **技术栈**：Vue 3 + TypeScript + Ant Design Vue + ECharts
- **组件设计**：采用组件化开发，每个图表独立封装为组件
- **数据请求**：使用Axios异步请求后端API，支持防抖和错误处理
- **响应式设计**：图表自适应不同屏幕尺寸，支持大屏展示
- **实时更新**：支持定时刷新数据，保持数据的实时性

### 后端实现
- **技术栈**：Spring Boot 3 + MyBatis Plus + MySQL
- **数据处理**：使用Stream API进行数据统计和分析
- **缓存策略**：对热点数据进行缓存，提高查询性能
- **API设计**：RESTful风格接口，返回标准化的JSON数据
- **权限控制**：基于角色的访问控制，只有管理员和设计师可访问

## （6）业务规则

1. **数据范围**：所有统计数据仅包含已通过审核且未删除的内容
2. **时间范围**：
   - 用户增长：近15天
   - 文章互动：近7天
   - 用户群体偏好：近7天
3. **排序规则**：
   - 热门风格：按作品数量降序
   - 作品互动量：按点赞数+评论数+收藏数降序
   - 文章互动：分别按点赞数、浏览量、收藏数降序
4. **数据更新频率**：实时数据，每次刷新页面时重新获取

## （7）性能优化

1. **数据缓存**：对热点统计数据进行Redis缓存，过期时间10分钟
2. **异步加载**：图表数据采用异步加载方式，先渲染页面框架，再加载数据
3. **数据分页**：对大量数据进行分页处理，只返回需要展示的数据
4. **图表懒加载**：非首屏图表采用懒加载方式，提高页面加载速度
5. **数据库索引**：对常用查询字段建立索引，提高查询效率

## （8）安全考虑

1. **权限控制**：基于RBAC权限模型，确保只有授权用户可访问
2. **数据脱敏**：所有敏感数据（如用户ID、具体用户信息）均进行脱敏处理
3. **接口限流**：对统计接口进行限流，防止恶意请求
4. **日志记录**：记录所有数据访问日志，便于审计和追溯
5. **HTTPS传输**：所有数据传输采用HTTPS协议，确保数据安全

## （9）后续功能建议

1. 支持自定义时间范围查询，提高数据查询的灵活性
2. 增加数据导出功能，支持导出Excel/PDF格式的报表
3. 支持图表联动，点击某一图表元素可触发其他图表的数据过滤
4. 增加数据预警功能，当关键指标异常时自动提醒管理员
5. 支持多维度数据钻取，从汇总数据深入查看明细数据