# 管理员端图案作品管理模块

## （1）功能说明

管理员端图案作品管理模块是服装图案智能创作平台的核心管理功能之一，主要负责对用户生成的服装图案进行全生命周期管理。该模块提供了图案列表查询、多条件搜索、审核操作（通过/拒绝）、详情查看、删除等核心功能，是平台内容审核机制的重要组成部分。通过该模块，管理员可以确保平台上展示的图案符合规范，维护平台内容质量，保障用户体验。

## （2）用例说明

管理员端图案作品管理模块服务于平台内容审核和管理的多个典型场景：

**用例1：图案审核**
- **角色**：平台管理员
- **场景**：平台收到大量用户上传的服装图案，需要及时审核确保内容合规
- **功能**：管理员通过待审核图案列表查看所有待处理的图案，对每个图案进行详细审查后执行通过或拒绝操作
- **价值**：确保平台内容符合法律法规和平台规范，防止违规内容传播

**用例2：违规图案处理**
- **角色**：平台管理员
- **场景**：用户举报或系统检测到违规图案，需要管理员进行处理
- **功能**：管理员通过搜索功能定位到特定图案，查看详情后执行删除操作
- **价值**：快速响应违规内容，维护平台良好生态

**用例3：图案数据统计与分析**
- **角色**：平台运营人员
- **场景**：需要了解平台图案生成情况，分析用户创作趋势
- **功能**：通过多条件筛选和排序，查看不同生成方式、风格、季节的图案分布
- **价值**：为平台运营决策提供数据支持，优化平台功能和服务

## （3）操作流程

### 3.1 图案列表查询与搜索
1. 管理员登录系统后进入图案管理页面
2. 系统默认加载所有图案的分页列表，显示图案ID、预览图、名称、生成方式、文件信息、审核状态、用户ID、创建时间、审核时间等信息
3. 管理员可以通过搜索区域的条件进行筛选：
   - 图案名称模糊搜索
   - 生成方式筛选（文字生成/图片参考生成）
   - 审核状态筛选（待审核/PENDING、已通过/APPROVED、已拒绝/REJECTED）
   - 用户ID精确搜索
4. 点击"搜索"按钮执行查询，点击"重置"按钮清空所有搜索条件
5. 点击"待审核图案"快捷按钮可直接筛选出所有待审核的图案
6. 点击"刷新"按钮可重新加载当前条件下的数据

### 3.2 图案审核操作
#### 3.2.1 审核通过
1. 管理员在图案列表中找到待审核（PENDING）的图案
2. 点击"通过"按钮
3. 系统向后端发送审核请求，包含图案ID和审核状态（APPROVED）
4. 后端更新图案的审核状态为已通过，记录审核时间和审核人ID
5. 前端刷新列表，图案状态更新为已通过

#### 3.2.2 审核拒绝
1. 管理员在图案列表中找到待审核（PENDING）的图案
2. 点击"拒绝"按钮，弹出拒绝原因输入弹窗
3. 管理员输入拒绝原因（最多200字符）
4. 点击"确认"按钮，系统向后端发送审核请求，包含图案ID、审核状态（REJECTED）和拒绝原因
5. 后端更新图案的审核状态为已拒绝，记录审核时间、审核人ID和拒绝原因
6. 前端刷新列表，图案状态更新为已拒绝，并显示拒绝原因

### 3.3 图案详情查看
1. 管理员在图案列表中找到需要查看详情的图案
2. 点击"详情"按钮，弹出详情弹窗
3. 弹窗中展示图案高清大图、基本信息（名称、描述、生成方式、风格、季节、目标受众）、文件信息（大小、类型）、审核状态、创建时间等完整信息
4. 管理员可详细查看图案内容，辅助审核决策

### 3.4 图案删除
1. 管理员在图案列表中找到需要删除的图案
2. 点击"删除"按钮
3. 系统向后端发送删除请求，包含图案ID
4. 后端执行软删除操作，将图案的isDelete字段标记为1
5. 前端刷新列表，该图案不再显示在列表中

## （4）数据库表字段

| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | Long | 主键ID，唯一标识一个图案 |
| userId | Long | 创作该图案的用户ID |
| patternName | String | 图案名称 |
| description | String | 图案描述 |
| generationType | String | 生成方式（TEXT_GENERATED/IMAGE_REFERENCED） |
| referenceImageUrl | String | 参考图片URL（图片参考生成时使用） |
| patternUrl | String | 图案主图URL |
| thumbUrl | String | 图案缩略图URL |
| fileSize | Integer | 文件大小（字节） |
| fileType | String | 文件类型（如PNG、JPG） |
| style | String | 图案风格（如简约、可爱、复古等） |
| season | String | 适用季节（如春季、夏季、四季通用等） |
| targetAudience | String | 目标人群（如儿童、成人、男性、女性等） |
| generationParams | Object | 生成参数（JSON格式，存储生成图案时的配置） |
| auditStatus | String | 审核状态（PENDING/APPROVED/REJECTED） |
| auditTime | Date | 审核时间 |
| auditorId | Long | 审核人ID |
| rejectReason | String | 拒绝原因（审核拒绝时必填） |
| likeCount | Integer | 点赞数 |
| createTime | Date | 创建时间 |
| updateTime | Date | 更新时间 |
| isDelete | Integer | 软删除标记（0-未删除，1-已删除） |

## （5）核心功能点

### 5.1 多条件搜索与筛选
- 支持按图案名称、生成方式、审核状态、用户ID进行组合搜索
- 提供待审核图案快捷筛选入口
- 支持重置搜索条件

### 5.2 审核工作流
- 清晰的审核状态标识（待审核、已通过、已拒绝）
- 便捷的审核操作按钮（通过/拒绝）
- 拒绝原因必填机制，确保审核透明度
- 支持已拒绝图案重新通过审核

### 5.3 可视化图案管理
- 图案列表展示缩略图，支持大图预览
- 详情弹窗展示完整图案信息
- 响应式设计，适配不同屏幕尺寸
- 加载状态提示，提升用户体验

### 5.4 高效的批量操作
- 分页加载机制，支持10/20/50/100条每页切换
- 支持按创建时间排序
- 实时更新待审核图案数量统计

## （6）技术实现要点

1. **前端技术栈**：Vue 3 + Ant Design Vue，采用组件化开发，确保代码可维护性和扩展性
2. **后端技术栈**：Spring Boot + MyBatis Plus，提供RESTful API接口
3. **数据库**：MySQL，使用MyBatis Plus进行ORM映射
4. **权限控制**：基于角色的访问控制（RBAC），确保只有管理员角色可以访问该模块
5. **状态管理**：使用Vue 3的Composition API管理组件状态
6. **异步请求**：采用Axios进行异步API调用，处理加载状态和错误信息
7. **软删除机制**：所有删除操作均为软删除，便于数据恢复和审计

## （7）业务规则

1. **审核状态流转**：
   - 新建图案默认状态为"待审核（PENDING）"
   - 待审核图案可流转为"已通过（APPROVED）"或"已拒绝（REJECTED）"
   - 已拒绝图案可重新流转为"已通过（APPROVED）"

2. **权限控制**：
   - 只有拥有管理员角色的用户才能访问该模块
   - 普通用户和设计师无法访问管理员功能

3. **数据安全性**：
   - 所有API接口均需经过权限验证
   - 敏感操作（如删除）需要二次确认
   - 操作日志记录，便于审计和追溯

4. **性能优化**：
   - 图案列表采用分页加载，避免一次性加载大量数据
   - 缩略图机制，提升列表加载速度
   - 搜索条件缓存，提升用户体验

## （8）后续功能建议

1. 添加图案审核日志功能，记录每次审核操作的详细信息
2. 实现图案批量审核功能，提高审核效率
3. 添加图案相似度检测功能，辅助管理员识别重复或相似图案
4. 提供图案数据导出功能，便于离线分析
5. 增加图案审核统计报表，展示审核通过率、审核时效等关键指标