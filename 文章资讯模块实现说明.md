# 文章资讯模块实现说明

## 📋 功能概述

文章资讯模块是一个完整的内容管理系统，支持文章的浏览、搜索、分类筛选、点赞、收藏等功能。

### 主要功能

1. **文章列表**
   - 分类筛选
   - 关键词搜索
   - 多维度排序（发布时间、阅读量、点赞量）
   - 分页浏览
   - 置顶、热门、推荐标签

2. **文章详情**
   - 文章内容展示
   - 阅读量统计
   - 点赞功能
   - 收藏功能
   - 分享功能
   - 相关推荐

3. **分类管理**
   - 分类列表
   - 分类增删改查
   - 分类启用/禁用

4. **缓存优化**
   - Redis缓存热门文章
   - Redis缓存推荐文章
   - Redis异步统计阅读量
   - 点赞/收藏状态缓存

## 🗂️ 文件结构

### 后端文件

```
clothingPattern-backend/
├── src/main/java/com/xhs/clothingpatternbackend/
│   ├── controller/
│   │   ├── ArticleController.java              # 文章控制器
│   │   └── ArticleCategoryController.java       # 分类控制器
│   ├── service/
│   │   ├── ArticleService.java                  # 文章服务接口
│   │   ├── ArticleCategoryService.java          # 分类服务接口
│   │   └── impl/
│   │       ├── ArticleServiceImpl.java          # 文章服务实现
│   │       └── ArticleCategoryServiceImpl.java  # 分类服务实现
│   ├── mapper/
│   │   ├── ArticleMapper.java                   # 文章Mapper
│   │   ├── ArticleMapper.xml                    # 文章Mapper XML
│   │   ├── ArticleCategoryMapper.java           # 分类Mapper
│   │   ├── ArticleLikeMapper.java               # 点赞Mapper
│   │   └── ArticleCollectMapper.java            # 收藏Mapper
│   ├── model/
│   │   ├── entity/
│   │   │   ├── Article.java                     # 文章实体
│   │   │   ├── ArticleCategory.java             # 分类实体
│   │   │   ├── ArticleLike.java                 # 点赞实体
│   │   │   └── ArticleCollect.java              # 收藏实体
│   │   ├── vo/
│   │   │   ├── ArticleVO.java                   # 文章VO
│   │   │   └── LikeResult.java                  # 点赞结果VO
│   │   └── dto/
│   │       └── article/
│   │           ├── ArticleQueryRequest.java     # 查询请求
│   │           └── ArticleAddRequest.java       # 添加请求
```

### 前端文件

```
clothingPattern-front/
├── src/
│   ├── pages/
│   │   ├── ArticlePage.vue                      # 文章列表页
│   │   └── ArticleDetailPage.vue                # 文章详情页
│   ├── api/
│   │   ├── articleController.ts                 # 文章API
│   │   └── typings.d.ts                         # 类型定义
│   └── router/
│       └── index.ts                             # 路由配置（已更新）
```

## 🎨 前端页面

### 1. 文章列表页 (`ArticlePage.vue`)

**路由**: `/article`

**功能特性**:
- 顶部Banner展示
- 分类导航（全部 + 自定义分类）
- 搜索框（支持标题、摘要、内容搜索）
- 排序选择（发布时间、阅读量、点赞量 × 升序/降序）
- 卡片式文章列表
- 响应式布局
- 分页功能

**技术亮点**:
- 使用 Ant Design Vue 组件库
- Grid布局实现响应式
- 图片懒加载优化
- 骨架屏加载效果
- dayjs格式化时间

### 2. 文章详情页 (`ArticleDetailPage.vue`)

**路由**: `/article/:id`

**功能特性**:
- 返回按钮
- 文章标题和元信息
- 文章统计数据（阅读、点赞、评论、收藏）
- 封面图展示
- 文章摘要
- 富文本内容展示
- 标签显示
- 操作按钮（点赞、收藏、分享）
- 相关推荐文章

**技术亮点**:
- 富文本内容样式优化
- 点赞/收藏状态实时更新
- 一键分享（复制链接）
- 相关推荐算法
- 页面SEO优化

## 🔌 API接口

### 文章接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取文章列表 | POST | `/article/list` | 支持分页、筛选、排序 |
| 获取文章详情 | GET | `/article/{id}` | 返回文章完整信息 |
| 搜索文章 | GET | `/article/search` | 关键词搜索 |
| 获取热门文章 | GET | `/article/hot` | 返回热门文章列表 |
| 获取推荐文章 | GET | `/article/recommend` | 返回推荐文章列表 |
| 添加文章 | POST | `/article/add` | 需要登录 |
| 更新文章 | POST | `/article/update` | 需要登录 |
| 删除文章 | POST | `/article/delete/{id}` | 需要登录 |
| 发布文章 | POST | `/article/publish/{id}` | 需要登录 |
| 下架文章 | POST | `/article/offline/{id}` | 需要登录 |
| 点赞文章 | POST | `/article/like` | 需要登录 |
| 收藏文章 | POST | `/article/collect` | 需要登录 |
| 取消收藏 | POST | `/article/collect/cancel` | 需要登录 |
| 获取点赞状态 | GET | `/article/like/status` | 需要登录 |
| 获取收藏状态 | GET | `/article/collect/status` | 需要登录 |

### 分类接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取可用分类 | GET | `/article/category/list` | 返回启用的分类 |
| 获取所有分类 | GET | `/article/category/all` | 返回所有分类 |
| 添加分类 | POST | `/article/category/add` | 管理员权限 |
| 更新分类 | POST | `/article/category/update` | 管理员权限 |
| 删除分类 | POST | `/article/category/delete/{id}` | 管理员权限 |

## 🗄️ 数据库表结构

### 1. article（文章表）

```sql
CREATE TABLE `article` (
  `id` bigint NOT NULL COMMENT '文章ID',
  `category_id` bigint DEFAULT NULL COMMENT '分类ID',
  `title` varchar(200) NOT NULL COMMENT '文章标题',
  `cover_image` varchar(500) DEFAULT NULL COMMENT '封面图',
  `summary` varchar(500) DEFAULT NULL COMMENT '文章摘要',
  `content` longtext COMMENT '文章内容',
  `author` varchar(100) DEFAULT NULL COMMENT '作者',
  `source` varchar(200) DEFAULT NULL COMMENT '来源',
  `tags` varchar(500) DEFAULT NULL COMMENT '标签，逗号分隔',
  `view_count` int DEFAULT '0' COMMENT '浏览量',
  `like_count` int DEFAULT '0' COMMENT '点赞量',
  `comment_count` int DEFAULT '0' COMMENT '评论量',
  `share_count` int DEFAULT '0' COMMENT '分享量',
  `collect_count` int DEFAULT '0' COMMENT '收藏量',
  `is_top` int DEFAULT '0' COMMENT '是否置顶：0-否，1-是',
  `is_hot` int DEFAULT '0' COMMENT '是否热门：0-否，1-是',
  `is_recommend` int DEFAULT '0' COMMENT '是否推荐：0-否，1-是',
  `status` varchar(20) DEFAULT 'DRAFT' COMMENT '状态：DRAFT-草稿，PUBLISHED-已发布，OFFLINE-已下架',
  `audit_status` varchar(20) DEFAULT 'PENDING' COMMENT '审核状态：PENDING-待审核，APPROVED-已通过，REJECTED-已拒绝',
  `publish_time` datetime DEFAULT NULL COMMENT '发布时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_delete` int DEFAULT '0' COMMENT '逻辑删除',
  PRIMARY KEY (`id`),
  KEY `idx_category_id` (`category_id`),
  KEY `idx_status` (`status`),
  KEY `idx_audit_status` (`audit_status`),
  KEY `idx_publish_time` (`publish_time`),
  KEY `idx_view_count` (`view_count`),
  KEY `idx_like_count` (`like_count`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章资讯表';
```

### 2. article_category（文章分类表）

```sql
CREATE TABLE `article_category` (
  `id` bigint NOT NULL COMMENT '分类ID',
  `category_name` varchar(100) NOT NULL COMMENT '分类名称',
  `category_desc` varchar(500) DEFAULT NULL COMMENT '分类描述',
  `icon` varchar(200) DEFAULT NULL COMMENT '分类图标',
  `sort_order` int DEFAULT '0' COMMENT '排序字段，越大越靠前',
  `status` int DEFAULT '1' COMMENT '状态：0-禁用，1-启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_delete` int DEFAULT '0' COMMENT '逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_category_name` (`category_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章分类表';
```

### 3. article_like（文章点赞表）

```sql
CREATE TABLE `article_like` (
  `id` bigint NOT NULL COMMENT '点赞ID',
  `article_id` bigint NOT NULL COMMENT '文章ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_delete` int DEFAULT '0' COMMENT '逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_article_user` (`article_id`, `user_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章点赞表';
```

### 4. article_collect（文章收藏表）

```sql
CREATE TABLE `article_collect` (
  `id` bigint NOT NULL COMMENT '收藏ID',
  `article_id` bigint NOT NULL COMMENT '文章ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_delete` int DEFAULT '0' COMMENT '逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_article_user` (`article_id`, `user_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章收藏表';
```

## 🚀 部署步骤

### 1. 数据库初始化

执行以下SQL创建表结构：

```bash
# 在MySQL中执行上述建表语句
mysql -u root -p your_database < article_tables.sql
```

### 2. 插入测试分类数据

```sql
INSERT INTO article_category (id, category_name, category_desc, sort_order, status) VALUES
(1, '时尚资讯', '最新的时尚动态和流行趋势', 100, 1),
(2, '穿搭指南', '专业的穿搭技巧和建议', 90, 1),
(3, '服装设计', '服装设计理念和创意分享', 80, 1),
(4, '面料知识', '各类面料的特性和选择', 70, 1),
(5, '品牌故事', '时尚品牌背后的故事', 60, 1);
```

### 3. 插入测试文章数据

```sql
INSERT INTO article (id, category_id, title, cover_image, summary, content, author, status, audit_status, publish_time, view_count, like_count, comment_count, collect_count, is_top, is_hot, is_recommend) VALUES
(1, 1, '2025春夏时尚趋势解析', 'https://example.com/cover1.jpg', '本文将为您详细解析2025年春夏季的时尚趋势...', '<h2>色彩趋势</h2><p>今年春夏的主流色彩...</p>', '时尚编辑', 'PUBLISHED', 'APPROVED', NOW(), 1280, 156, 23, 89, 1, 1, 1),
(2, 2, '职场穿搭必备单品TOP10', 'https://example.com/cover2.jpg', '为职场人士精选的10件必备单品，让你轻松打造专业形象...', '<h2>单品推荐</h2><p>1. 经典白衬衫...</p>', '穿搭顾问', 'PUBLISHED', 'APPROVED', NOW(), 856, 92, 15, 67, 0, 1, 1),
(3, 3, '如何设计一件完美的连衣裙', 'https://example.com/cover3.jpg', '从灵感到成品，详细讲解连衣裙的设计流程...', '<h2>设计灵感</h2><p>设计的第一步是寻找灵感...</p>', '设计师', 'PUBLISHED', 'APPROVED', NOW(), 645, 78, 12, 45, 0, 0, 1);
```

### 4. 后端配置

确保 `application.yml` 中配置了Redis：

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    database: 0
```

### 5. 启动服务

**后端**:
```bash
cd clothingPattern-backend
mvn clean package
java -jar target/clothingPattern-backend.jar
```

**前端**:
```bash
cd clothingPattern-front
npm install
npm run dev
```

### 6. 访问应用

- 文章列表页：http://localhost:5173/article
- 文章详情页：http://localhost:5173/article/1

## 🧪 功能测试

### 1. 文章列表测试

✅ 访问 `/article` 页面
✅ 查看文章卡片展示
✅ 点击分类筛选
✅ 输入关键词搜索
✅ 切换排序方式
✅ 翻页功能

### 2. 文章详情测试

✅ 点击文章卡片进入详情
✅ 查看文章内容
✅ 点击点赞按钮
✅ 点击收藏按钮
✅ 点击分享按钮
✅ 查看推荐文章

### 3. 未登录测试

✅ 未登录时可以浏览文章
✅ 未登录时点赞/收藏提示登录

### 4. 响应式测试

✅ 在不同屏幕尺寸下测试布局
✅ 移动端适配测试

## 📊 性能优化

### 1. 缓存策略

- **热门文章**：Redis缓存1小时
- **推荐文章**：Redis缓存1小时
- **阅读量**：Redis异步累加，定时同步数据库
- **点赞状态**：Redis Hash存储
- **收藏状态**：Redis Hash存储

### 2. 查询优化

- 数据库添加索引（category_id, status, publish_time等）
- 批量查询点赞/收藏状态
- 分页查询减少数据传输
- MyBatis XML动态SQL优化

### 3. 前端优化

- 图片懒加载
- 路由懒加载
- 组件按需引入
- 防抖搜索
- 骨架屏加载

## 🔧 扩展功能建议

### 1. 文章管理后台

- 文章发布/编辑界面
- 富文本编辑器集成
- 图片上传功能
- 文章审核流程

### 2. 评论系统集成

- 复用现有的评论模块
- 在文章详情页添加评论区

### 3. 标签系统

- 标签云展示
- 标签筛选
- 热门标签推荐

### 4. 用户互动

- 文章浏览历史
- 用户收藏夹
- 阅读进度记录

### 5. 内容推荐

- 基于用户兴趣推荐
- 协同过滤算法
- 个性化推送

## 📝 注意事项

1. **数据库表必须先创建**
   - 执行建表SQL
   - 插入测试数据

2. **Redis必须启动**
   - 文章模块依赖Redis缓存
   - 确保Redis连接正常

3. **路由已自动配置**
   - `/article` - 文章列表
   - `/article/:id` - 文章详情

4. **富文本内容安全**
   - 后端需要对content字段进行XSS过滤
   - 前端使用`v-html`需谨慎

5. **图片资源**
   - 测试数据中的图片URL需要替换为真实地址
   - 建议集成COS或OSS存储

## 🐛 常见问题

### Q1: 文章列表显示空白

**解决方案**:
1. 检查数据库是否有数据
2. 检查文章status='PUBLISHED'且audit_status='APPROVED'
3. 检查is_delete=0

### Q2: 点赞/收藏失败

**解决方案**:
1. 检查是否已登录
2. 检查Redis连接
3. 查看浏览器控制台错误信息

### Q3: 图片不显示

**解决方案**:
1. 检查cover_image字段是否有值
2. 确认图片URL可访问
3. 检查跨域配置

### Q4: 分页不工作

**解决方案**:
1. 检查total值是否正确
2. 检查pageNum和pageSize参数
3. 查看后端SQL是否正确

## 📞 技术支持

如有问题，请查看：
- 后端代码：`ArticleController.java`, `ArticleServiceImpl.java`
- 前端代码：`ArticlePage.vue`, `ArticleDetailPage.vue`
- API文档：`articleController.ts`

---

**开发日期**：2025-11-26
**版本**：v1.0
**功能模块**：文章资讯系统

