# 衣图融合模块功能描述

## 1. 模块概述

衣图融合模块是一个基于AI技术的智能服装设计工具，允许用户将服装款式图与图案素材进行智能融合，生成全新的服装效果图。该模块通过调用AI生成服务，实现图案与服装的自然、美观融合，为设计师和用户提供高效的设计辅助。

## 2. 核心功能

### 2.1 图片上传功能

#### 2.1.1 服装图上传
- 支持JPG、JPEG、PNG、BMP格式图片
- 单张图片大小限制：≤5MB
- 图片尺寸要求：384-5000像素
- 提供本地预览功能
- 支持重新上传

#### 2.1.2 图案图上传
- 支持JPG、JPEG、PNG、BMP格式图片
- 单张图片大小限制：≤5MB
- 图片尺寸要求：384-5000像素
- 最多可上传2张图案图片
- 提供本地预览功能
- 支持删除和重新上传

### 2.2 AI融合参数设置

#### 2.2.1 原图相似度调节
- 滑块式调节，范围0-100
- 实时显示当前相似度值
- 影响融合结果与原图的相似度

### 2.3 融合任务管理

#### 2.3.1 任务提交
- 自动将本地图片上传至COS存储
- 调用AI融合服务生成任务ID
- 支持批量图片处理
- 固定融合提示词：确保图案自然融合到服装上

#### 2.3.2 任务状态查询
- 实时轮询任务状态（每3秒）
- 支持任务状态显示：处理中、成功、失败
- 失败时显示错误信息

#### 2.3.3 结果展示
- 生成结果大图展示
- 支持图片预览
- 支持结果保存

### 2.4 结果管理

#### 2.4.1 结果保存
- 保存融合结果到用户图库
- 支持单张图片保存
- 保存状态反馈

#### 2.4.2 历史记录
- 支持分页查询历史融合任务
- 支持按条件筛选
- 支持查看历史结果

## 3. 技术实现

### 3.1 前端实现

- **框架**：Vue 3 + TypeScript
- **UI组件库**：Ant Design Vue
- **核心功能**：
  - 图片上传与预览
  - 相似度滑块控制
  - 任务状态轮询
  - 结果展示与保存

### 3.2 后端实现

- **框架**：Spring Boot + MyBatis Plus
- **核心功能**：
  - 图片上传至COS存储
  - AI融合任务提交与管理
  - 任务状态查询与结果处理
  - 结果图片持久化
  - 缓存机制（本地缓存 + Redis）

### 3.3 AI服务集成

- 通过DashScope API调用AI融合服务
- 固定融合提示词模板，确保融合效果
- 支持异步任务处理

### 3.4 数据存储

- **数据库**：MySQL
- **表结构**：
  - `image_fusion_task`：存储融合任务信息和结果
- **图片存储**：腾讯云COS

## 4. 业务流程

### 4.1 融合生成流程

1. 用户上传服装图和图案图
2. 调整原图相似度参数
3. 点击"智能生成"按钮
4. 前端验证图片合法性
5. 上传图片至COS存储
6. 提交融合任务至AI服务
7. 轮询任务状态
8. 展示融合结果
9. 用户选择保存结果

### 4.2 结果保存流程

1. 用户点击"保存图案"按钮
2. 后端验证任务和图片信息
3. 保存图片到用户图库
4. 返回保存结果

## 5. 界面设计

### 5.1 布局结构

- **左侧面板**：操作区域
  - 服装图上传
  - 图案图上传
  - 相似度调节
  - 生成按钮
- **右侧面板**：结果展示区域
  - 加载状态
  - 结果大图
  - 保存按钮
  - 空状态示例

### 5.2 交互设计

- 实时验证图片格式和大小
- 上传成功/失败反馈
- 任务处理中加载动画
- 结果生成成功提示
- 保存成功/失败反馈

## 6. 性能优化

- **图片上传**：本地预览，延迟上传
- **任务轮询**：智能间隔，成功后停止
- **缓存机制**：本地缓存 + Redis缓存，减少数据库查询
- **图片处理**：AI服务处理，减轻服务器压力

## 7. 边界情况处理

- 图片格式不支持
- 图片大小超限
- 图片尺寸不符合要求
- AI服务调用失败
- 任务处理超时
- 网络异常

## 8. 扩展功能

- 支持更多图片格式
- 增加更多融合参数调节
- 支持批量生成和对比
- 增加风格迁移功能
- 支持融合结果编辑

## 9. 适用场景

- 服装设计师快速生成设计稿
- 电商平台商品图生成
- 服装定制服务
- 服装设计教学
- 服装搭配参考

## 10. 核心优势

- 操作简单，用户友好
- AI驱动，效果自然
- 实时反馈，体验流畅
- 结果可保存，便于管理
- 支持多种图片格式和尺寸

---

## 11. 模块架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        衣图融合模块                              │
├───────────────┬─────────────────────────────────────────────────┤
│   前端界面    │  上传区域      |  参数调节    |  结果展示        │
├───────────────┼─────────────────────────────────────────────────┤
│   后端服务    │  图片上传      |  任务管理    |  结果处理        │
├───────────────┼─────────────────────────────────────────────────┤
│   AI服务      │  图案融合      |  结果生成                        │
├───────────────┼─────────────────────────────────────────────────┤
│   数据存储    │  任务数据      |  图片存储    |  历史记录        │
└───────────────┴─────────────────────────────────────────────────┘
```

## 12. 关键API接口

| 接口路径 | 方法 | 功能描述 |
|---------|------|---------|
| `/image-fusion/upload` | POST | 上传图片至COS |
| `/image-fusion/submit` | POST | 提交融合任务 |
| `/image-fusion/status/{taskId}` | GET | 查询任务状态 |
| `/image-fusion/results/{taskId}` | GET | 获取融合结果 |
| `/image-fusion/save-selected` | POST | 保存选中图片 |
| `/image-fusion/list/page/vo` | POST | 分页查询融合任务 |

---

## 13. 版本信息

- **当前版本**：1.0
- **更新日期**：2025-12-20
- **主要更新**：
  - 初始版本发布
  - 支持服装图与图案图融合
  - 支持相似度调节
  - 支持结果保存和历史记录查询