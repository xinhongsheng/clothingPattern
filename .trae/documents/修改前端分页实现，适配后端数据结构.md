## 问题分析
1. 当前前端代码使用`res.data.data.list`获取数据列表，但后端返回的是MyBatis-Plus的Page对象，使用`records`字段
2. 用户提供了一个示例，展示了如何使用`searchParams`和`pagination`来实现分页，需要将ArticlePage和ArticleManagePage的分页实现改为这种方式
3. 需要修改两个文件：`ArticlePage.vue`和`ArticleManagePage.vue`

## 修复计划
1. **修改`ArticlePage.vue`**：
   - 将`loadArticles`方法中的`res.data.data.list`改为`res.data.data.records`
   - 保持其他逻辑不变

2. **修改`ArticleManagePage.vue`**：
   - 将当前的分页实现改为与用户提供的示例保持一致
   - 使用`searchParams`对象管理分页参数
   - 使用`pagination`计算属性生成分页配置
   - 修改`doTableChange`和`doSearch`方法

## 具体修改
1. **修改`ArticlePage.vue`**：
   - 第199行：`articleList.value = res.data.data.list || []` → `articleList.value = res.data.data.records || []`

2. **修改`ArticleManagePage.vue`**：
   - 将`searchParams`对象改为使用`current`和`pageSize`字段，而不是`pageNum`和`pageSize`
   - 添加`pagination`计算属性
   - 修改`handleTableChange`方法为`doTableChange`
   - 修改`handleSearch`方法为`doSearch`
   - 修改`loadArticles`方法中的参数传递

## 预期效果
1. 前端代码能够正确接收和处理后端返回的Page<ArticleVO>数据
2. 分页功能正常工作，能够正确显示数据列表和总记录数
3. 代码结构与用户提供的示例保持一致，提高可维护性
4. 编译成功，没有类型错误

## 注意事项
1. 确保修改后的代码与用户提供的示例保持一致
2. 保持代码风格与现有代码一致
3. 测试分页功能是否正常工作
4. 测试搜索功能是否正常工作