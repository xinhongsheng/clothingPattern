# 改进文章列表查询接口，提高代码可维护性

## 任务分析

需要改进文章列表查询接口，提高代码的可维护性。当前的代码结构如下：

1. **ArticleQueryRequest** - 继承了PageRequest，包含分页参数和查询条件
2. **PageResult** - 封装分页结果，包含列表数据和总记录数
3. **getArticleList接口** - 实现了分页查询功能

## 改进目标

1. 提高代码的可维护性和可读性
2. 封装列表转换逻辑
3. 优化查询和分页逻辑
4. 保持原有功能不变

## 实现步骤

### 1. 添加getArticleVOList方法

在ArticleServiceImpl中添加`getArticleVOList`方法，用于将Article列表转换为ArticleVO列表，提高代码复用性。

**代码实现**：
```java
/**
 * 将Article列表转换为ArticleVO列表
 */
private List<ArticleVO> getArticleVOList(List<Article> articleList) {
    if (CollUtil.isEmpty(articleList)) {
        return new ArrayList<>();
    }
    return articleList.stream().map(this::convertToVO).collect(Collectors.toList());
}

/**
 * 将Article转换为ArticleVO
 */
private ArticleVO convertToVO(Article article) {
    ArticleVO vo = new ArticleVO();
    BeanUtils.copyProperties(article, vo);
    // 查询分类名称
    ArticleCategory category = articleCategoryService.getById(article.getCategoryId());
    if (category != null) {
        vo.setCategoryName(category.getCategoryName());
    }
    return vo;
}
```

### 2. 改进getArticleList方法

优化`getArticleList`方法，使用更清晰的代码结构，提高可读性。

**代码实现**：
```java
@Override
public PageResult<ArticleVO> getArticleList(ArticleQueryRequest query, Long currentUserId) {
    // 1. 构建分页对象
    Page<Article> page = new Page<>(query.getPageNum(), query.getPageSize());
    
    // 2. 使用getQueryWrapper方法构建查询条件
    QueryWrapper<Article> queryWrapper = getQueryWrapper(query);

    // 3. 执行分页查询
    Page<Article> articlePage = this.page(page, queryWrapper);
    
    // 4. 转换为VO列表
    List<ArticleVO> articleVOList = getArticleVOList(articlePage.getRecords());
    
    // 5. 批量设置用户交互状态
    if (currentUserId != null && !articleVOList.isEmpty()) {
        setUserInteractionStatus(articleVOList, currentUserId);
    }
    
    // 6. 构建分页结果
    PageResult<ArticleVO> pageResult = new PageResult<>();
    pageResult.setList(articleVOList);
    pageResult.setTotal(articlePage.getTotal());
    
    return pageResult;
}
```

### 3. 优化setUserInteractionStatus方法

确保`setUserInteractionStatus`方法的实现正确，用于设置用户的点赞和收藏状态。

### 4. 保持控制器方法不变

控制器方法保持不变，继续使用现有的实现：

```java
@PostMapping("/list")
public BaseResponse<PageResult<ArticleVO>> getArticleList(@Valid @RequestBody ArticleQueryRequest query,
        HttpServletRequest request) {
    // 获取当前用户ID（未登录用户为null）
    Long currentUserId = null;
    try {
        User loginUser = userService.getLoginUser(request);
        if (loginUser != null) {
            currentUserId = loginUser.getId();
        }
    } catch (Exception e) {
        // 未登录，保持currentUserId为null
    }

    PageResult<ArticleVO> result = articleService.getArticleList(query, currentUserId);
    return ResultUtils.success(result);
}
```

## 技术要点

1. **封装转换逻辑**：将列表转换逻辑封装在单独的方法中，提高代码复用性
2. **清晰的代码结构**：使用注释和分步骤实现，提高代码可读性
3. **保持原有功能**：确保改进后功能不变
4. **优化查询逻辑**：使用getQueryWrapper方法构建查询条件
5. **使用Hutool工具类**：简化代码，提高开发效率

## 预期效果

1. 代码结构更清晰，可读性提高
2. 列表转换逻辑复用性提高
3. 便于后续扩展和维护
4. 原有功能保持不变
5. 分页查询正常工作

## 测试建议

1. 测试文章列表查询功能
2. 测试分页功能
3. 测试多条件过滤功能
4. 测试排序功能
5. 测试用户交互状态（点赞、收藏）

## 实现时间

预计需要15-20分钟完成所有修改和测试。