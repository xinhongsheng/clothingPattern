# 运行时错误修复说明

## 🐛 错误1：MyBatis找不到XML映射

### 错误信息
```
org.apache.ibatis.binding.BindingException: Invalid bound statement (not found): 
com.xhs.clothingpatternbackend.mapper.ArticleMapper.selectArticleList
```

### 原因分析
MyBatis 无法找到 `ArticleMapper.xml` 文件，因为 `application.yml` 中没有配置 mapper XML 文件的扫描路径。

### 解决方案

**修改文件：** `application.yml`

**添加配置：**
```yaml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    local-cache-scope: statement
  global-config:
    db-config:
      logic-delete-field: isDelete
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath:com/xhs/mapper/*.xml  # ✅ 新增这一行
```

### 说明
- XML 文件实际位置：`src/main/resources/com/xhs/mapper/ArticleMapper.xml`
- MyBatis 需要明确指定 XML 文件的扫描路径
- `classpath:` 表示从 resources 目录开始
- `*.xml` 表示扫描该目录下所有 XML 文件

---

## 🐛 错误2：前端函数名错误

### 错误信息
```
加载分类失败: ReferenceError: getCategories is not defined
```

### 原因分析
`ArticlePage.vue` 中调用的函数名错误。API 文件中导出的是 `getArticleCategories`，而不是 `getCategories`。

### 解决方案

**修改文件：** `ArticlePage.vue`

**修改代码：**
```typescript
// ❌ 错误的函数名
const res = await getCategories()

// ✅ 正确的函数名
const res = await getArticleCategories()
```

### 完整修复代码
```typescript
// 加载分类
const loadCategories = async () => {
  try {
    const res = await getArticleCategories()  // ✅ 修改这里
    if (res.data.code === 0 && res.data.data) {
      categories.value = res.data.data
    } else {
      message.error('加载分类失败：' + res.data.message)
    }
  } catch (error: any) {
    console.error('加载分类失败:', error)
    message.error('加载分类失败：' + error.message)
  }
}
```

---

## ✅ 修复验证

### 后端验证

1. **重启后端服务**
   ```bash
   # 停止当前运行的服务（Ctrl+C）
   # 重新启动
   mvn spring-boot:run
   ```

2. **检查日志**
   - 应该看到 MyBatis 成功加载了 XML 文件
   - 不应再有 "Invalid bound statement" 错误

3. **测试 API**
   ```bash
   # 测试分类接口
   curl http://localhost:8123/api/article/category/list
   
   # 测试文章列表接口
   curl -X POST http://localhost:8123/api/article/list \
     -H "Content-Type: application/json" \
     -d '{"pageNum":1,"pageSize":10,"status":"PUBLISHED","auditStatus":"APPROVED"}'
   ```

### 前端验证

1. **刷新浏览器**
   - 访问：http://localhost:5173/article
   
2. **检查控制台**
   - 不应有 "getCategories is not defined" 错误
   
3. **检查页面**
   - ✅ 分类导航正常显示
   - ✅ 文章列表正常加载
   - ✅ 文章卡片正常渲染

---

## 📋 修改文件汇总

| 文件 | 修改内容 | 状态 |
|------|---------|------|
| `application.yml` | 添加 `mapper-locations` 配置 | ✅ 已修复 |
| `ArticlePage.vue` | 修改函数名 `getCategories` → `getArticleCategories` | ✅ 已修复 |

---

## 🎯 测试清单

### 后端测试
- [ ] 后端服务成功启动
- [ ] MyBatis 成功加载 XML 文件
- [ ] 分类接口返回数据
- [ ] 文章列表接口返回数据
- [ ] 文章详情接口正常工作

### 前端测试
- [ ] 页面加载无错误
- [ ] 分类导航正常显示
- [ ] 文章列表正常显示
- [ ] 点击分类可以筛选
- [ ] 搜索功能正常
- [ ] 排序功能正常
- [ ] 点击文章可以跳转详情页

---

## 💡 常见问题

### Q1: 重启后端后还是报错？
**A:** 检查以下几点：
1. 确认 `application.yml` 修改已保存
2. 确认 XML 文件在 `src/main/resources/com/xhs/mapper/` 目录下
3. 清理并重新编译：`mvn clean compile`

### Q2: 前端还是报错？
**A:** 检查以下几点：
1. 确认浏览器已刷新（Ctrl+F5 强制刷新）
2. 清空浏览器缓存
3. 重启前端服务：`npm run dev`

### Q3: 数据库没有测试数据？
**A:** 执行初始化脚本：
```bash
mysql -u root -p your_database < clothingPattern-backend/文章模块数据库初始化.sql
```

---

## 🚀 现在可以正常使用了！

修复完成后，你应该能够：
1. ✅ 访问文章列表页面
2. ✅ 查看文章分类
3. ✅ 浏览文章内容
4. ✅ 搜索和筛选文章
5. ✅ 点击文章查看详情

如果还有问题，请查看：
- 后端日志：查看具体错误信息
- 浏览器控制台：查看前端错误
- Network 标签：查看 API 请求状态

---

**修复日期：** 2025-11-26  
**修复状态：** ✅ 全部完成

